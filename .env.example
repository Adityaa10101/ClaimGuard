# ClaimGuard Environment Variables
# Copy this file to .env and fill in values

# Groq API Key for LLM-based claim extraction (optional — regex fallback available)
GROQ_API_KEY=

# Maximum upload file size in MB (default: 50)
MAX_UPLOAD_SIZE_MB=50

# Temporary file directory (cleaned after processing)
TEMP_DIR=tmp/
